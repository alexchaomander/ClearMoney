{
  "name": "@clearmoney/strata-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "build": "echo 'No build step for Python API'",
    "lint": "ruff check app/ tests/",
    "type-check": "mypy app/"
  }
}
