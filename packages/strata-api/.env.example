# Strata API Configuration
STRATA_DATABASE_URL=postgresql+asyncpg://<user>:<password>@<host>:<port>/strata
STRATA_DATABASE_ECHO=false
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
STRATA_CREDENTIALS_ENCRYPTION_KEY=
STRATA_DEBUG=false

# Redis (optional for local dev, recommended for production)
STRATA_REDIS_URL=

# Sentry (optional, error tracking)
STRATA_SENTRY_DSN=

# SnapTrade Configuration
# Get these from https://snaptrade.com/developers
STRATA_SNAPTRADE_CLIENT_ID=
STRATA_SNAPTRADE_CONSUMER_KEY=
